name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    name: Build and test on Linux
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build
        run: ./build.sh

      - name: Run tests
        run: ./test.sh
